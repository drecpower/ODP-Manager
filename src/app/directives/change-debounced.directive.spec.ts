import { ChangeDebouncedDirective } from './change-debounced.directive';

describe('ChangeDebouncedDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeDebouncedDirective();
    expect(directive).toBeTruthy();
  });
});
